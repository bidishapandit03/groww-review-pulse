GROWW REVIEW PULSE — PHASE PLAN (1-9)
=====================================
Stack: Next.js + TypeScript on Vercel (PRD Option C). All phases committed to git
on `main`. Raw review data is NEVER committed (lives in Vercel Blob in prod,
data/raw/ locally — both gitignored).

------------------------------------------------------------------------
PROGRESS AT A GLANCE
------------------------------------------------------------------------
 1 Repo scaffold        DONE  9042d2c
 2 Import reviews       DONE  bebe62e
 3 Redact PII (CI gate) DONE  dab8662
 4 Theme legend+goldset DONE  a31ccf3
 5 Mistral tagging      DONE  015a1a1
 6 Top themes+quotes    DONE  (phase 6 commit)
 7 Weekly note          NEXT
 8 Email draft          NEXT
 9 App + hosting+cron   NEXT

========================================================================
DONE — PHASE 1: Repo scaffold (PRD Step 1)
========================================================================
Commit: 9042d2c "chore: scaffold Next.js project and pipeline infra"

- Next.js 16 + React 19 (TS strict). lib/constants.ts (theme legend, limits,
  window, models, step order), lib/types.ts (Review/RunMeta schemas),
  lib/state.ts (Vercel Blob in prod / data/raw local JSON in dev),
  lib/dates.ts (toDateOnly, weeksAgoIso, isoWeek, isoYearWeek),
  .env.example (secret names only), vercel.json cron (Mon 09:00 UTC),
  AGENTS.md (PRD hard rules).

========================================================================
DONE — PHASE 2: Import reviews (PRD Step 2)
========================================================================
Commit: bebe62e "feat(import): fetch reviews from both public stores"

- lib/imports/appstore.ts — Apple public RSS (India, id 1404871703), ~500 reviews.
- lib/imports/playstore.ts — google-play-scraper, ~2000 reviews, no login.
- lib/imports/index.ts — pure functions: fetchFreshReviews() -> mergeCorpus() ->
  sliceWindow() (per-store coverage = oldest date).
- scripts/smoke-import.ts — manual diagnostic.
- Corpus at data/raw/pipeline/corpus.json (2500 reviews, raw, gitignored).
- Coverage ~2.5-3 weeks back; corpus grows each weekly run toward 8 weeks.

========================================================================
DONE — PHASE 3: Clean data and redact PII (PRD Step 3)
========================================================================
Commit: dab8662 "feat(redact): clean & redact PII with CI gate"

- lib/redact.ts — idempotent redactText / redactReviews / containsPii: URLs,
  emails, phones (+91 & plain & spaced), PAN, Aadhaar, 12+ digit runs,
  UPI IDs, @handles -> [REDACTED].
- scripts/pii-gate.ts — scans tracked data/sample + outputs; CI failure on hit.
- .github/workflows/ci.yml — typecheck + vitest + pii:gate on push/PR.
- lib/pipeline/types.ts — storage keys, PipelineState, AggregateResult,
  CandidateQuote, pipelineRunSchema.
- Refactored imports into pure functions so each pipeline step owns its state.

========================================================================
DONE — PHASE 4: Theme legend + gold set (PRD Step 4)
========================================================================
Commit: a31ccf3 "feat(goldset): theme legend confirmed + 50-review gold set"

Theme legend (lib/constants.ts), locked + hidden "other" bucket:
  1. onboarding_kyc       — Demat opening, KYC/e-sign delays, verification
  2. payments_withdrawals — UPI add-funds failures, bank linking, payouts, SIP
  3. trading_orders       — Order failures, market-hours lag, F&O, charts, data
  4. charges_statements   — Brokerage, P&L/tax statements, contract notes
  5. app_support          — Crashes, login/OTP, slowness, support response

- scripts/gold-set.ts — seeded stratified sampler (over-samples 1-star),
  redacts before writing. Usage: npx tsx scripts/gold-set.ts [count] [seed].
- data/sample/gold-set.json — 50 hand-labeled entries, PII-free, committed.
  Spread: other 27 | app_support 8 | charges 7 | trading 6 | payments 1 | kyc 1.
- prompts/tag_themes.txt — verbatim theme definitions + tie-break rules + JSON contract.
- tests/goldset.test.ts — CI guards (size, unique ids, valid labels, both
  stores, all ratings, zero PII, every theme present).
- Labels are a draft for user review (edit gold-set.json, keep tests green).

========================================================================
DONE — PHASE 5: Tag themes with Mistral (PRD Step 5)
========================================================================
Commit: 015a1a1 "feat(tag): theme tagging with Mistral — 92% gold-set accuracy"

- lib/llm.ts — Mistral transport: auth guard, JSON mode, retry/backoff (429 via
  statusCode), content-chunk flattening.
- lib/types.ts — tagThemeSchema (5 + other), taggedReviewSchema (Review+theme).
- lib/pipeline/tag.ts — tagReviews() batches (40/batch) REDACTED reviews;
  redaction happens INSIDE the tag path before any prompt (PRD FR2).
  parseTagResponse validates every theme with zod; malformed reply fails loudly.
- scripts/eval-tag.ts — accuracy vs gold set: npx tsx scripts/eval-tag.ts [min].
  Writes data/sample/tagged-gold.json. npm run tag:eval.
- tests/tag.test.ts — 14 pure tests, no network.

MODEL SWITCH (account tier): mistral-small-latest -> 429, mistral-large-latest
-> 403 on this free tier. Available: open ministral models. Both constants now
ministral-8b-latest (see lib/constants.ts comment). Upgrade plan to restore.

Measured accuracy: 92% (46/50). kyc/payments/trading 100%, charges 86%,
app_support 63%, other 100%. Misses were ambiguous reviews, not clear errors.

========================================================================
DONE — PHASE 6: Top 3 themes + candidate quotes (PRD Step 6)
========================================================================
Commit: 91874bb "feat(aggregate): rank top themes + shortlist quotes"

Ranking formula (computed in code, weights tunable):
  Impact = 0.5 * volume + 0.3 * negativity + 0.2 * momentum
    volume     = count / max(count over themes)   -> "how many users"
    negativity = share of 1-3 star reviews        -> "how angry"
    momentum   = clamped positive WoW growth (7d vs prior 7d) -> "how urgent"
  Ties break by count. "other" never competes or surfaces.

Quote policy (decision): include clear praise AND Hinglish if understandable.
  Complaints rank first in the pool; praise stays eligible. Word bound 4-25.

Built:
- lib/pipeline/aggregate.ts — computeThemeStats, rankThemes, shortlistQuotes,
  aggregate(), wowDelta() (all pure; no LLM).
- lib/dates.ts — daysAgoIso(). lib/constants.ts — QUOTE_MIN_WORDS=4,
  QUOTE_MAX_WORDS=25, QUOTE_CANDIDATES_PER_THEME=6.
- tests/aggregate.test.ts — 9 math tests (wow, rank, momentum, quote filter).
- scripts/smoke-aggregate.ts — npm run aggregate:smoke (runs on tagged gold set;
  verified real verbatim Hinglish quotes). NOTE: numbers are gold-set-only,
  not the real window.

========================================================================
NEXT — PHASE 7: Generate + validate weekly note (PRD Step 7)
========================================================================
Goal: one-page pulse with EXACTLY 3 themes, 3 verbatim quotes, 3 action ideas,
<=250 words. LLM writes prose; code guarantees every exact value.

Work:
- prompts/write_note.txt — note prompt: theme labels, counts, avg rating,
  WoW delta, candidate quote IDs with text. Model SELECTS quote IDs; code
  inserts the exact quote text (quotes always verbatim + exist in data).
- lib/pipeline/note.ts — mistralChat (ministral-8b-latest for now), JSON mode,
  zod validation of the note schema.
- lib/types.ts — Note schema: weekLabel, 3 themes (label/count/avg/wow),
  3 quote ids, 3 action strings, generatedAt, wordCount.
- Validators (retry once with error message on failure):
  word count <=250 | exactly 3/3/3 | quotes exist in corpus | no PII.
- Outputs: outputs/weekly-note-YYYY-WW.md + NOTE_KEY.

Open decisions: note tone (neutral-factual vs urgent)? owners + metrics per
action? include one-line WoW comparison within word limit?

========================================================================
NEXT — PHASE 8: Create the email draft (PRD Step 8)
========================================================================
Goal: the note as a Gmail DRAFT (never sent) in the user's mailbox.

Work:
- scripts/authorize-gmail.ts — one-time OAuth -> refresh token.
- lib/pipeline/email.ts — googleapis Gmail drafts.create, note as body;
  subject e.g. "Groww Weekly Review Pulse - Week 38, 2026".
- Secrets: GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN/RECIPIENT in Vercel env /
  .env.local (.env.example documents names only).
- Fallback: .eml file or mailto: link + screenshot in repo.

Open decisions: which email address? Google Cloud OAuth or .eml fallback?

========================================================================
NEXT — PHASE 9: App, hosting, automation (PRD Step 9)
========================================================================
Goal: a live product a reviewer can open plus scheduled weekly runs.

Work:
- app/api/pipeline/[step]/route.ts — one endpoint per step; each calls the
  independent function in lib/.
- app/api/cron/pulse/route.ts — orchestrator running steps in order, updating
  PipelineState (state machine already in lib/pipeline/types.ts).
- app/page.tsx — replace default template: public "latest pulse" page (zero
  PII) + admin run UI guarded by ADMIN_PASSWORD (optional).
- vercel.json cron already set: 0 9 * * 1 → /api/cron/pulse.
- CI already live (typecheck + tests + pii:gate).

Open decisions: keep weekly cron or manual "Run" only? public or protected UI?

========================================================================
CROSS-CUTTING RULES (AGENTS.md)
========================================================================
- Pipeline steps = independent functions in lib/; endpoints in app/api/
  pipeline/[step]/route.ts; orchestrator in app/api/cron/pulse/route.ts.
- State via lib/state.ts (Blob prod / local JSON dev).
- External contracts validated with zod. ALL prompt text in prompts/*.txt.
- Anything exact (counts, averages, WoW, word count, quote text) computed in
  code, never by the LLM.
- No PII ever; redact before any LLM call or file write. Raw never committed.
- Secrets only in Vercel env / .env.local; .env.example documents names only.

========================================================================
REPO MAP
========================================================================
app/                    Next.js app (default template; phase 9 builds UI)
data/raw/               gitignored local state (corpus.json etc.)
data/sample/            committed REDACTED only: gold-set.json, tagged-gold.json
lib/imports/            appstore.ts, playstore.ts, index.ts (pure)
lib/pipeline/           types.ts, tag.ts, aggregate.ts
lib/redact.ts           PII masking
lib/llm.ts              Mistral transport
lib/state.ts            Blob / local JSON store
lib/constants.ts        themes, limits, ranking weights, models
lib/dates.ts            date helpers (daysAgoIso, weeksAgoIso, isoWeek...)
lib/types.ts            Review, TaggedReview, TagTheme schemas
prompts/                tag_themes.txt (phase 4); write_note.txt (phase 7)
scripts/                smoke-import, pii-gate, gold-set, eval-tag, smoke-aggregate,
                        (phase 8: authorize-gmail.ts)
outputs/                committed note artifacts (weekly-note-YYYY-WW.md)
tests/                  redact, goldset, tag, aggregate (+ phase 7 note tests)
.github/workflows/      ci.yml (typecheck + test + pii:gate)
vercel.json             cron Mon 09:00 UTC -> /api/cron/pulse