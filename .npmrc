# Resolve peer-dependency conflicts during npm install (Vercel uses npm i).
# Our dependency tree (e.g. google-play-scraper transitives vs React 19) has
# non-blocking peer conflicts; align Vercel with our CI's `--legacy-peer-deps`.
legacy-peer-deps=true